# Shared

Edit files and run build scripts to use it.

1. pnpm --filter shared build
2. pnpm install

### Usage

Use it by adding the `@repo/shared` in your script's pacakage.json

```json
"dependencies": {
    "@repo/shared": "workspace:*"
  }
```
